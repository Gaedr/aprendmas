<!DOCTYPE html>
<html lang="es">
    <head>
        <meta charset="UTF-8" />
        <title>aprenD+ :: {% block title %}{% endblock %}</title>
        <link rel="stylesheet" type="text/css" href="{{ asset('css/bootstrap.min.css') }}" />
        <link rel="stylesheet" type="text/css" href="{{ asset('css/aprendmas.css') }}" />
        {#<link rel="stylesheet" type="text/css" href="{{ asset('css/jquery-ui-timepicker.addon.min.css') }}" />#}
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        {% block stylesheets %}{% endblock %}
         <script src="{{asset('js/jquery-1.11.1.min.js') }}" type="text/javascript"> </script>  
         <script src="{{asset('js/bootstrap.min.js') }}" type="text/javascript"></script>
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>        
        <div class="container-fluid">
            {% if app.user.username is defined %}
            <nav class="navbar navbar-default">
                 <div class="container-fluid">
                     <div class="navbar-header">
                        <a class="navbar-brand" href="{{ path('perfil') }}">Perfil de {{ app.user.username }}</a>
                     </div>
                     <div class="collapse navbar-collapse">
                        <ul class="nav navbar-nav">
                            <li><a href="{{ path('alta_concepto') }}">Conceptos</a></li>
                            <li><a href="{{ path('alta_actividad')}}">Actividades</a></li>
                            <li><a href="{{ path('alta_alumno')}}">Alta Alumno</a></li>
                        </ul>                         
                        <ul class="nav navbar-nav navbar-right">
                           <li><a href="{{ path('logout') }}">[Salir]</a></li>
                        </ul>
                       {# <ul class="nav navbar-nav navbar-right">
                            <li class="dropdown">
                                <a href="#" class="dropdown-toggle" data-toggle="dropdown">{{ app.user.username }} <b class="caret"></b> </a>
                                <ul class="dropdown-menu">
                                    <li><a href="{{ path('logout') }}">Salir</a></li>
                                </ul>
                            </li>
                        </ul>#}
                     </div>
                 </div>
             </nav>
            {% endif %}
            <div id='content' class="row">
                <h1>{{ block('title')}}</h1>
                <div {%if block('menu') %} class="col-md-8" {%else%} class="col-md-12" {% endif %}>
                    {% block body %}{% endblock %}
                </div>                  
                {% if block('menu')%}
                <div id="menuDerecha" class="col-md-3 offset8">
                {% block menu %}{% endblock %}
                </div>
                {% endif %}
            </div>     
        {% block footer %}{% endblock %}
        </div>
        
        
         
        <script type="text/javascript" src="{{ asset('js/ajax.js') }}"></script>
        {#<script type="text/javascript" src="{{ asset('js/jquery-ui-sliderAcces.js')}}"></script>
        <script type="text/javascript" src="{{ asset('js/jquery-ui-timepicker-addon.min.js')}}"></script>        
        <script type="text/javascript">
            $(document).ready(function() {
                $('.dropdown-toggle').dropdown('toggle');
            });
        </script>#}
        {% block javascripts %}{% endblock %}
    </body>
</html>
